#ifndef enum_h
#define enum_h

// Enum -> quickSort , mergeSort , heapSort i introSort

enum sortingAlgorithm {
    
    quickSort = 1,
    mergeSort = 2,
    heapSort  = 3,
    introSorting = 4,
    
};

#endif /* enum_h */
